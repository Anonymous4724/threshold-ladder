__pycache__/
*.py[cod]
.vscode/
.idea/
.DS_Store
Thumbs.db

# Publishing this particular copy: which account, which repository, which two
# clicks. Operating notes for whoever owns the site, not documentation of the
# project — and a page explaining how to put a site online has no business
# being on that site. They stay on disk and keep working; they just do not ship.
publish.py
publish.bat
*.local.md

# What the live feed serves; it never lives in the repository.
live.json
